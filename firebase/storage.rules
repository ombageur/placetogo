rules_version = '2';

// Deny-by-default. Avatar kustom (jika kelak didukung) ditambahkan di Fase 03
// dengan validasi kepemilikan, tipe konten, dan ukuran.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
